"""Steward CLI — typer entry points.

Subcommands live as siblings: scan, inspect, policy, apply, stash, db, import.
``cli.main:app`` is the console-script entry point declared in pyproject.toml.
"""
